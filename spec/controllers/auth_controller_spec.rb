require "rails_helper"

RSpec.describe AuthController, :controller do
  describe ".callback" do
    context "if successful" do
      it "saves the remember_token to the user's cookie store"

      it "redirects the user to releases#index"
    end
  end
end